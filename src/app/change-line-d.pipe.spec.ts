import { ChangeLineDPipe } from './change-line-d.pipe';

describe('ChangeLineDPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeLineDPipe();
    expect(pipe).toBeTruthy();
  });
});
